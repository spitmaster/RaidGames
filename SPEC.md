# SPEC — 游戏大厅（RaidGames / GameLobby）

> **本文件是项目唯一真相源（Single Source of Truth）。** 任何代码实现必须满足这里的验收标准；需求变更先改本文件再改代码。
> 需求来源与决策记录见 [requirements.md](requirements.md)。
> 版本：v0.1（草案） · 日期：2026-05-27 · 状态：待评审

---

## 1. 项目概述

- **项目名称**：游戏大厅（插件 ID：`GameLobby`，仓库 `RaidGames`）
- **项目简介**：一个魔兽世界 WotLK Classic 插件。团本结束后，团长/助理在「游戏大厅」发起小游戏，全团参与，凭比赛结果决定战利品归属，替代单纯 roll 点。首发游戏「极速按键」。
- **目标用户**：中国魔兽世界时光服（WLK）团队/公会玩家。
- **项目背景**：roll 点纯看脸，输家不服气；用手速类小游戏给一个「凭本事翻盘」的机会，排名与战绩带来正反馈。
- **核心设计目标 — 低门槛传播（一等目标）**：大厅本体与每款小游戏都能以 WeakAuras 字符串分享，**复制即用、免装插件、新游戏免重装**。这是项目的首要目标，直接决定了下文的「双分发同体」架构（§5），不是附属功能。参考同类插件 BiaoGe 已验证此模式可行（`sample/BiaoGe/Core/Module/AuctionWA.lua`）。

---

## 2. 功能需求

### 功能 1：大厅主框架与角色识别

- **描述**：插件提供一个可开关的主面板，识别当前玩家是否为团长/助理，决定能否发起比赛。
- **用户故事**：作为团长，我希望打开大厅就能看到可发起的游戏列表，以便快速组织一场比赛。

**验收标准**：
- [ ] 斜杠命令 `/gl` 或 `/gamelobby`（及中文 `/游戏大厅`）切换主面板显隐。
- [ ] 通过 `UnitIsGroupLeader("player")` 与 `UnitIsGroupAssistant("player")` 判定身份；非团长/助理时「发起」按钮禁用并提示「仅团长/助理可发起」。
- [ ] 主面板列出已注册游戏：极速按键（可用）、是男人就下 100 层（灰色·即将推出）、是男人就上 100 层（灰色·即将推出）。
- [ ] 不在队伍/团队中时，发起按钮禁用并提示「需在队伍或团队中」。

### 功能 2：发起比赛

- **描述**：团长/助理选择游戏并发起，向全团广播开赛。
- **用户故事**：作为团长，我希望点一下就能把比赛推送给全团，以便所有人同时参与。

**验收标准**：
- [ ] 发起时生成唯一 `matchId`（如 `时间戳-发起者名`）。
- [ ] 发起前可设**奖品模式**（见 §4.3 ①、§4.7 LootCard）：①真实战利品（稀有度着色）②团长自定义文字奖品（≤60 字，如「5000 金币」）③友谊赛（无奖品）。
- [ ] 通过插件通讯频道（团队优先，无团队则队伍）广播 `Start`（含 gameId/版本/奖品，协议见 §6）。
- [ ] 发起者成为本场「裁判（host）」，负责收集分数与汇总排名。
- [ ] 同一时刻只允许一场进行中的比赛；已有进行中比赛时再次发起被拒绝并提示。

### 功能 3：参与邀请、准备与开始倒计时

- **描述**：团长发起后团员收到邀请，进入大厅点「准备」；团长见全员就绪后开局，统一倒计时。
- **用户故事**：作为团员，我希望被清晰邀请并能表态「准备」，以便团长掌握谁要参赛。

**验收标准**：
- [ ] 收到 `Start` 后，团员弹出邀请（StaticPopup）：「{发起者} 发起了【极速按键】，是否参与？」按钮：参与（打开大厅）/ 围观。
- [ ] 选择「参与」进入大厅，可点「准备」（广播 `Join` + 就绪态）；大厅显示就绪计数 `✓ readyCount/totalMembers`（见 §4.3 ②④）。
- [ ] 团长侧显示「等待 N 人准备 / ✓ 全员就绪」，点「开始比赛」广播开局；所有参与者同步进入 `3→2→1→GO!` 倒计时遮罩（见 §4.4）。
- [ ] 报名/准备有截止保护（默认开赛后 N 秒），逾期未就绪视为围观。
- [ ] 无插件/WA 的团员不会响应；裁判端统计并显示「X 人无插件无法参与」。

### 功能 4：极速按键 — 比赛核心

- **描述**：10 秒计时窗口内，狂点屏幕上的大按钮，记录点击次数。
- **用户故事**：作为参与者，我希望在 10 秒内尽可能多地点击按钮，以便刷出高分。

**验收标准**：
- [ ] 比赛窗口含：① 醒目大按钮；② 实时点击计数；③ 剩余时间倒计时条。
- [ ] 计时窗口默认 **10.0 秒**（可配置），到点后按钮立即失效、停止计数。
- [ ] 用 `OnMouseDown`（而非仅 `OnClick`）计数，确保每次按下都计入；窗口外的点击不计。
- [ ] 计时使用游戏时钟（`GetTime()`），不依赖帧率，误差 ≤ ±0.1 秒。
- [ ] 结束后自动通过 `Result` 上报本人最终点击数。
- [ ] 比赛中显示 castbar 计时条 + 实时排名 live-board（自适应列），自己高亮、第 1 名高亮（见 §4.5）。
- [ ] 围观者看到同样的窗口但按钮不可用，仅观战。

### 功能 5：结果汇总与排名公示

- **描述**：裁判端收集所有上报分数，排名后向全团广播最终结果。
- **用户故事**：作为团长，我希望比赛一结束就看到清晰排名和冠军，以便据此分配战利品。

**验收标准**：
- [ ] 裁判端在计时结束后留 3 秒收集窗口接收 `Result`。
- [ ] 按点击数**降序**排名，并列名次处理见功能 6。
- [ ] 裁判广播 `Final`（含完整排名 + 冠军）；所有客户端显示结算屏：胜利者横幅 + 奖品归属条 + 最终榜（含 **CPS=点击数/时长**，第 1 名 ✦、2 银 3 铜，自己加「(你)」，见 §4.6）。
- [ ] 结算屏操作：[关闭]；团长另有 [再来一局]。
- [ ] 可选（默认开）：裁判向团队频道喊话「【游戏大厅·极速按键】冠军：{名字}（{点击数} 次）！」并按奖品模式公示归属（战利品/自定义/友谊赛文案见 §4.6）。
- [ ] 上报分数做上限校验：超过 `时长(秒) × 单秒最大点击数(默认 20)` 的分数标记为「异常」并不计入冠军。

### 功能 6：平局 — 突然死亡加赛

- **描述**：冠军位出现并列点击数时，触发 5 秒加赛。
- **用户故事**：作为参与者，平局时我希望有一个干净利落的加时决胜，以便分出胜负。

**验收标准**：
- [ ] 仅当**第一名**出现并列时触发；非冠军位的并列按同名次处理，不触发加赛。
- [ ] 裁判广播 `Tie`（含并列者名单），只有并列者进入 5 秒加赛窗口，其余人观战。
- [ ] 加赛仍并列则继续下一轮 5 秒加赛，直至分出唯一冠军。
- [ ] 加赛结果同样走 `LOBBY_RESULT` → `LOBBY_FINAL` 流程。

### 功能 7：本地个人战绩

- **描述**：按账号在本地记录个人战绩。
- **用户故事**：作为玩家，我希望看到自己的历史最高分和胜率，以便有成就感和复玩动力。

**验收标准**：
- [ ] SavedVariables 按**账号**（非单角色）累计对战记录；战史屏顶部 5 个统计卡：**总场次 / 胜场 / 胜率 / 奖品收获 / 平均分**（见 §4.7）。
- [ ] 入口为标题栏「战史」按钮；战史屏列出对战记录（时间/游戏/奖品/胜者/我的结果），可滚动，胜局高亮。
- [ ] 每场比赛结束后自动写入本人战绩（参与即记录，无论输赢），记录含奖品（战利品/自定义/友谊赛三态）。
- [ ] 提供清空个人战绩的操作（带二次确认）。

### 功能 8：跨客户端通讯

- **描述**：插件与 WA 用户通过统一协议在同一场比赛中互通。
- **用户故事**：作为团队，我希望有人装插件、有人用 WA 也能一起玩同一场，以便降低参与门槛。

**验收标准**：
- [ ] 统一通讯前缀与消息格式（见 §6），插件版与 WA 版严格一致。
- [ ] 优先 `C_ChatInfo.SendAddonMessage`，不可用时回退旧版全局 `SendAddonMessage`。
- [ ] 注册通讯前缀（`C_ChatInfo.RegisterAddonMessagePrefix`）。
- [ ] 协议带版本号字段，版本不兼容时给出可读提示。
- [ ] 通讯频道：在团队用 `RAID`，仅队伍用 `PARTY`。

### 功能 9：动态分享与扩展（传播性核心）

- **描述**：游戏/大厅以**单一 WeakAuras 格式字符串**分享（一串通吃）、复制即用、免重装。**两个导入入口、同一种串**：
  - **WeakAuras 用户**：把串粘进 WeakAuras 导入。
  - **纯插件用户**：把同一串粘进**大厅自带的「导入框」**——大厅内置 WA 串解析，提取游戏代码并注册，**不依赖 WeakAuras**。
- **用户故事**：作为玩家，我把一段字符串发给任何朋友，不管他用 WeakAuras 还是只装了大厅，粘贴即玩，以便最大化扩散。

**验收标准**：
- [ ] 游戏字符串**统一用 WeakAuras 格式**（`!WA:` 前缀）；一种串，两类用户都能用。
- [ ] 大厅插件提供「导入游戏字符串」入口，**内置 WA 串解析**（base64 + `LibDeflate` + `LibSerialize`，库内嵌；参考 `sample/BiaoGe/Libs/LibBase64`），从 WA 表的自定义代码字段提取游戏 → 注册 → **立即可玩，无需 /reload、无需 WeakAuras**。
- [ ] WeakAuras 用户把同一串粘进 WeakAuras，也能导入并注册进大厅（需先有大厅核心，或核心本身也以 WA 提供）。
- [ ] 大厅本体获取方式：插件安装 / WeakAuras 字符串（给纯 WA 用户）。
- [ ] 导入更高版本的同游戏串 → 版本门控自动热升级替换旧版（见 §5）。
- [ ] 同一游戏不论来源（子插件 / 字符串），`gameId` + 协议一致，可同场互通（见功能 8）——**联机互通靠协议，与字符串格式无关**。
- [ ] 缺某游戏或版本不一致 → 提示「你缺少该游戏 / 版本过旧，向对方索取游戏字符串」，不静默失败。
- [ ] 安全（见 §3）：导入携带可执行代码 → **必须弹确认**「此游戏含可执行代码，仅从可信来源导入，是否信任运行？」默认拒绝。WeakAuras 入口由 WA 自带确认兜；大厅入口由大厅**自建确认门**。无法真正沙箱，仅靠"可信来源"约束。
- [ ] **我们自控游戏 WA 模板**（游戏代码放固定字段），降低对 WA 内部结构变化的耦合；WA 大改版时适配解析逻辑。
- [ ] **不做游戏内一键推送 / P2P 代码传输**（见 requirements 非目标）；分享靠复制粘贴字符串。

### 功能 10：关于 / About（联系作者 + 赞助）

- **描述**：标题栏提供「关于」入口，点开显示作者联系方式（便于反馈 bug）与可选赞助二维码。
- **用户故事**：作为用户，我发现 bug 时希望能联系到作者；作为作者，我希望留个赞助入口。

**验收标准**：
- [ ] 标题栏「关于」按钮（与「战史」并列，见 §4.2），点开「关于」面板（沿用窗体视觉，见 §4.7b）。
- [ ] 显示：插件名 + 版本号 + 作者；联系方式（QQ 群 / 微信 / 邮箱 / B 站等，由作者填入）。
- [ ] 联系方式放**只读 EditBox**，便于用户 `Ctrl+C` 复制。
- [ ] 可选赞助区：二维码 / 收款码。
- [ ] **WA 限制**：二维码图片不随 WA 字符串传输（WA 不含二进制贴图）。WA 版改为 ① 显示收款链接/文字，或 ② Lua 矩阵渲染二维码（纯代码可随 WA 走）；插件版可直接用 `Media/` 贴图。

### 占位功能（M1 不实现，仅预留）

- **是男人就下 100 层** / **是男人就上 100 层**：在游戏注册表中占槽位，UI 显示「即将推出」，无可玩逻辑。

---

## 3. 非功能需求

- **性能要求**：比赛期间 UI 流畅，计数无丢失；通讯消息小且节流，不刷屏不被系统丢弃；20 人团队规模下排名汇总 ≤ 1 秒（收集窗口除外）。
- **安全 / 公平要求**：**防作弊为非目标**。分数客户端自报，仅做上限校验（见功能 5）；明确告知用户结果依赖团队社交信任。不读取/上传任何账号外数据。**字符串分发的代码安全**：游戏字符串携带可执行 Lua，运行在 WoW 沙箱内（无文件/操作系统访问），但能做插件能做的事（冒名发言、改数据等）。两个导入入口各自把"信任门"：① WeakAuras 入口由 WeakAuras 自带的「含代码，是否信任」确认兜；② **大厅自带导入框由大厅自己 `loadstring` 执行外来代码，因此必须自建确认门**（显示来源 + 强警告 + 默认拒绝）。两者都**无法真正沙箱**外来代码——用户指引须写明「只从可信来源获取游戏字符串」。
- **兼容性要求**：目标客户端时光服 `3.80.1.67621`（WLK 内容），TOC `## Interface: 38001`（据 BiaoGe 实测）。现代 API（`C_ChatInfo` / `C_Timer` / `Item` / `GetClassColor` / `ChatThrottleLib`）齐全，无需旧版兜底。中文客户端优先。**不依赖 BigFoot / 大脚**。
- **可用性要求**：全中文界面；操作路径短（发起 ≤ 2 次点击）；弹窗与结果信息清晰；无插件用户有明确提示。
- **主题**：设计提供 3 套配色主题（见 §4.1）。**M1 只实现默认铁木 Ironwood**；主题切换（奥术 / 血色 + 切换存档）降级到后续里程碑。

---

## 4. UI/UX 规范

> 本节对齐用户提供的 Claude Design 设计稿（handoff bundle：`sample/RaidGames-handoff/`，源样式 `project/styles.css`、屏幕 `project/screens.jsx`、组件 `project/components.jsx`）。设计为 HTML/CSS 原型，WoW 端按视觉还原，不照搬其代码结构。颜色为 hex，WoW Lua 需转 0–1 RGB。

### 4.1 主题与设计令牌

整体风格：暗色 + 暴雪式金属边框窗体（多层 box-shadow 描边 + 四角菱形宝石）。设计提供 **3 套主题**，默认 **铁木 Ironwood**（**M1 只实现铁木**，奥术 / 血色为后续里程碑，令牌先在此留存）：

| 令牌 | 铁木 Ironwood（默认） | 奥术 Arcane | 血色 Crimson | 用途 |
|------|------|------|------|------|
| `--bg-page` | `#0b0805` | `#060814` | `#0c0303` | 页面底色（径向渐变到 `--bg-page-2`） |
| `--panel` | `#1a140c` | `#0c1028` | `#1a0808` | 面板底 |
| `--panel-inset` | `#0a0703` | `#050715` | `#080202` | 内嵌凹槽底 |
| `--frame-bright` | `#d6a85b` | `#88c8ff` | `#e6b063` | 边框高光（金属亮边） |
| `--frame-mid` | `#6b4a22` | `#3a4faf` | `#7a1f1f` | 边框中间色 |
| `--divider` | `#4a341a` | `#2a3a78` | `#582020` | 分隔线 |
| `--text` | `#f3e6cb` | `#e6efff` | `#f6e2c8` | 正文 |
| `--text-dim` | `#b89a6a` | `#8aa5d4` | `#c98a72` | 次要文字 |
| `--text-mute` | `#7a6a48` | `#5a6c9a` | `#7a4a40` | 弱化文字 |
| `--accent` | `#f0c46c` | `#6ad6ff` | `#f0b860` | 强调（标题/高亮） |
| `--accent-glow` | `#ffd98a` | `#b8eaff` | `#ffd58a` | 强调高光（数字/冠军） |
| `--accent-deep` | `#8a5a1c` | `#1a3a78` | `#6a1a1a` | 强调暗部（发光阴影） |
| `--danger` | `#d85a3a`（三主题共用） | — | — | 危险/关闭 |
| `--success` | `#6abf5e`（三主题共用） | — | — | 就绪/成功 |

**稀有度色**（通用 RPG 约定，三主题共用）：普通 `#bcbcbc` · 优秀 `#4ec24c` · 精良 `#4a8ce8` · 史诗 `#a64ad8` · 传说 `#ff9b30`。

**职业色**（玩家名/卡片左边框，9 职业）：战士 `#c4945a` · 法师 `#66d6ee` · 牧师 `#e6e1d4` · 盗贼 `#f0e26a` · 圣骑 `#f0a4c8` · 猎人 `#a8d96a` · 术士 `#a884e0` · 萨满 `#4a8ee6` · 德鲁伊 `#ff8a3d`。

**字体**：标题/按钮 `Cinzel` + `Noto Serif SC`（serif，宽字距 0.2–0.5em，常大写/拉开）；正文 `Noto Sans SC`；数字/计时/分数 `JetBrains Mono`（`tabular-nums`）。WoW 端无 Cinzel，标题改用游戏自带衬线/中文字体，**保留宽字距与发光质感**。

### 4.2 全局窗体（AddonFrame）

```
addon（宽 min(1040px, 视口-64)，多层金属描边 + 四角菱形宝石 18px）
├── title-bar（高 56px）
│   ├── 左上角色徽章：★ 团长（金）/ 团员（灰）
│   ├── 居中标题「游 戏 大 厅」（Cinzel 22px，字距 0.4em，accent 色 + 发光）
│   │   └── 副标题「GAME · HALL · v0.1」（10px，text-mute，字距 0.5em）
│   ├── 两侧 title-flair 横线 + ◆
│   └── 右上：[战 史] + [关 于] 按钮（可高亮 is-active）+ [✕] 关闭
└── addon-body（padding 22/24/24，min-height 460px）→ 承载下面 5 个屏幕
```

### 4.3 屏幕 1 — 大厅 LobbyScreen

竖向分区（gap 16px），每区前有 `section-label`（11px 大写衬线 + 渐隐横线）：

```
① 奖品区  SectionLabel 文案随模式变：
   战利品「本次战利品 · Loot In Dispute」/ 自定义「本局奖品 · Custom Prize」/ 友谊赛「比赛模式 · Match Mode」
   └── LootCard（见 4.7）：战利品卡 / 自定义奖品输入卡 / 友谊赛卡
② 参赛者  「参赛者  N 人  ✓ readyCount/totalMembers」
   └── players 网格（5 列）：每个 PlayerCard（职业色左边框、自己高亮、团长 ★、就绪 ✓/未就绪 ○）
③ 选择游戏  game-tiles 网格（auto-fill minmax 170px）
   └── 每 tile：glyph(36px) + 标题 + 两行描述；可选(is-selected 金框) / 锁定(is-locked 0.45 透明 + 「即将上线」角标)
④ 操作行（右对齐）
   - 团员：状态文字「请点击准备 / 已准备 · 等待团长开局」+ [准 备 / 取消准备] 按钮
   - 团长：状态「等待 N 人准备 / ✓ 全员就绪」+ [开 始 比 赛] 大按钮（btn-lg，未选可用游戏时禁用）
⑤ log-strip 日志条（系统/战团/警告 标签行）
```

### 4.4 屏幕 2 — 倒计时（覆盖层 Countdown Overlay）

不是独立屏，而是**盖在比赛布局之上**的全屏遮罩（`countdown-overlay`，背景 `rgba(0,0,0,0.55→0.85)` + `blur(2px)`）：巨大数字 `3 → 2 → 1 → GO!`（Cinzel，180px，accent 色 + 多层发光，`cdpop` 弹入动画；GO! 转 success 绿 120px），下方副标题「准 备 · 极 速 按 键」。比赛主按钮此时 disabled 显示「就 位」。

### 4.5 屏幕 3 — 比赛 PlayingScreen（极速按键）

```
① castbar 横向进度条（高 24px，金色渐变填充 + 前缘火花 + 颗粒纹理）
   左标签「极 速 按 键」 右计时「X.Xs」（mono 13px，accent-glow）
② play-hero（三列：奖品 | 狂点钮 | 计数）
   - 左：play-mini-loot 迷你奖品卡（稀有度色，标「争 夺 中」）/ 友谊赛显「友 谊 赛」
   - 中：smash-btn 圆形大按钮 220×220（径向金渐变 + 4px 亮边，按下 is-pressed 下沉缩放 + 强发光）
         文案「点 击」（倒计时中「就 位」），下方提示「SPACE · CLICK · TAP」
   - 右：my-count 计数（mono 64px，accent-glow + 发光）+ 「我的次数 · 第 X 名」
③ 实时排名  「实时排名  N 人激战」
   └── live-board（自适应列：≤6 人 1 列 / ≤12 人 2 列 / 否则 3 列）
       每 live-row：名次 + 名字（职业色）+ 分数（mono）+ 底部进度条；第 1 名 rank-1 传说橙高亮；自己 is-self 金底
```

### 4.6 屏幕 4 — 结算 ResultsScreen

```
① winner-banner 胜利者横幅（径向金光底 + accent 边框 + 发光）
   「— 胜 利 者 —」+ 冠军名（Cinzel 38px，职业色/accent-glow）
   + 「以 N 次按键夺得奖品 / 技压全场」
② loot-award 归属条（顶/底分隔线）
   战利品：「战 利 品 已 归 属  <物品>  →  <冠军>」
   自定义：「奖 品 已 归 属  <奖品>  →  <冠军>」
   友谊赛：「友 谊 赛 · 纯 切 磋  <冠军>  赢 得 本 场 荣 誉」
③ final-board 最终榜（自适应列同 live-board）
   每 final-row：名次（第1名 ✦ 传说橙 / 2 银 / 3 铜）+ 名字（自己加「(你)」）+ 「CPS X.X」+ 总分
④ 操作行：[关 闭]  团长另有 [再 来 一 局]（btn-primary）
```

### 4.7 屏幕 5 — 战史 HistoryScreen

```
① history-stats 5 个统计卡（stat-card）：总场次 / 胜场(金) / 胜率(≥30% 金) / 奖品收获(蓝) / 平均分
   每卡：大数字（Cinzel 26px）+ 小标签（10px 大写字距）
② 「对战记录  最近 N 局」
   └── history-list（可滚动，max-height 360px）
       每 history-row（5 列）：时间 | 游戏(glyph+名+人数) | 奖品(稀有度色 glyph+名/友谊赛) | 胜者(职业色名+分) | 结果徽章（胜=金徽章 / 第N名=灰徽章）+ 次数
       胜局整行 is-win 金色左边框 + 微金渐变底
```

### 4.7b 关于面板 About

```
关于面板（沿用窗体视觉）
├── 标题「关 于 · About」
├── 插件名 + 版本号 + 作者
├── 联系方式区（section-label「联系作者 · 反馈 Bug」）
│   └── 只读 EditBox（QQ群/微信/邮箱/B站，可 Ctrl+C 复制）
└── 赞助区（可选，section-label「赞助 · Sponsor」）
    └── 二维码/收款码（插件版贴图；WA 版用链接文字或矩阵渲染）
```

### 4.8 组件规范（关键尺寸/状态）

| 组件 | 关键样式 | 状态 |
|------|----------|------|
| `btn` 普通按钮 | Cinzel 14px 大写，金属渐变底 + 内高光，padding 10/22 | hover 亮边发光 / active 下沉 1px / disabled 0.4 灰度 |
| `btn-primary` | 金渐变实底，深色字，强发光 | hover brightness 1.1 |
| `btn-lg` / `btn-sm` | 16px / 11px | — |
| `game-tile` 游戏格 | glyph 36px + 标题 13px + 描述 11px | default / is-selected（金框金光）/ is-locked（0.45 灰 + 「即将上线」角标） |
| `player` 玩家卡 | 左 3px 职业色边框 + 圆形首字头像 22px + 名字 + 状态 | is-self 金底 / is-leader ★ / is-ready ✓(success) |
| `loot-card` 战利品卡 | 60px 物品图标（稀有度色边 + 发光 + 角宝石）+ 名(16px 稀有度色)/类型·部位·品质/属性 | 按 data-rarity 着色 |
| `loot-custom` 自定义奖品卡 | 团长见 `prize-input` 输入框（≤60 字，占位提示）+ 字数；团员见只读奖品名 | 有奖品 has-prize 金框 / 留空=友谊赛虚线 |
| `castbar` 计时条 | 高 24px，金渐变填充 + 前缘火花 | width 随剩余时间 |
| `smash-btn` 狂点钮 | 圆形 220×220，径向金渐变 + 4px 亮边 | is-pressed 下沉缩放强发光 / disabled 灰 |
| `live-row` / `final-row` | 网格行，职业色左边框，分数 mono | rank-1 传说橙高亮 / is-self 金底 |
| `stat-card` 统计卡 | 顶部渐变细线 + 大数字 + 标签 | stat-gold 金 / stat-rare 蓝 |
| `log-strip` 日志条 | mono 11px，标签 系统(金)/战团/警告(红) | 最多 ~70px 高 |

### 4.9 交互与动效

- 狂点钮：`onMouseDown` 即计数（不等 click）；按下 70ms 内显示 `is-pressed` 反馈。设计同时允许空格/触摸触发（WoW 端以鼠标点击为主，见 §2 功能 4 / 决策 D1）。
- 倒计时数字 `cdpop` 弹入（scale 0.4→1.1→1 + 模糊消除）。
- 计时条 `width` 0.05s 线性过渡；实时排名行进度条 0.2s 缓动。
- 关闭按钮 hover 转 `--danger` 红。

### 4.10 WoW 还原注意

- 设计画布 1040px 偏大，WoW 端窗体可缩放但**保留布局结构与视觉语言**（金属边框、宝石角、字距、发光）。
- 无 CSS 的部分（box-shadow 多层描边、conic-gradient、blur）在 WoW 用贴图 + 多 Texture 层近似；圆形狂点钮用圆形贴图。
- 颜色 hex → 0–1 RGB；字体回退到游戏自带中文字体。

---

## 5. 技术架构

> 架构直接参考已验证的同类插件 **BiaoGe（金团表格）**：其 `Core/Module/AuctionWA.lua` 用一套代码同时跑插件和 WA，并支持靠 WA 字符串热升级功能模块——正是本项目要的形态。下文带 `BiaoGe:` 的引用均指 `sample/BiaoGe/`。

### 技术栈

| 类别 | 技术 |
|------|------|
| 语言 | Lua 5.1（WoW 嵌入） |
| 界面 | 纯 Lua 建框架 + XML 模板 |
| 平台 API | 时光服客户端 `## Interface: 38001`（3.80.x，WLK 内容 + 现代 API） |
| 持久化 | SavedVariables（账号级） |
| 通讯 | `C_ChatInfo.SendAddonMessage` / `RegisterAddonMessagePrefix`；`ChatThrottleLib` 节流 |
| 分发 | ① 独立插件（.toc）；② WeakAura 导入字符串。**同一份核心代码，两种渠道** |

### 双分发「同体」机制（核心，照搬 BiaoGe）

1. **运行环境自识别**：核心代码用 `local self = aura_env or {}` 判断——`aura_env` 存在即跑在 WeakAura 里，为 nil 即跑在插件里。（`BiaoGe: AuctionWA.lua:23`）
2. **全局命名空间幂等 + 版本门控**：建立 `_G.GameLobby`，已存在时比版本号——旧版本直接 `return` 让位，新版本先卸载旧实例（隐藏框架、`UnregisterAllEvents`、`wipe`）再接管。（`BiaoGe: AuctionWA.lua:30-49`）
   - 效果：插件和 WA 同时装也只有一个实例生效（高版本胜）；**可以靠粘贴更新的 WA 字符串热升级，不必更新整包**——这是「传播性」目标（§1、功能 9）的技术底座。
3. **加载引导**：仿 BiaoGe 的 `BG.Init` 队列（`BiaoGe: Init.lua:52-90`），用 `ADDON_LOADED` / `PLAYER_ENTERING_WORLD` / `PLAYER_LOGIN` 三档延迟初始化；WA 侧借 aura 自身加载时机。

### 框架 + 可插拔游戏

- 核心暴露注册接口 `GameLobby:RegisterGame(def)`，`def` 至少含 `{ id, name, version, host, client, onResult }`。
- 每个游戏（含极速按键）**同样用「同体」机制**：既能作为子插件被核心 `.toc` 加载并注册，也能作为一段 WA 字符串导入并注册（导入即注册，无需 /reload，见功能 9）。
- **游戏与通讯解耦**：游戏只产出「分数 / 事件」，收发统一走核心 `Comm`；新增游戏不碰通讯层。
- **可选集成、优雅降级**：游戏代码用 `if GameLobby then ... end` 检测核心是否在场（对应 BiaoGe 的 `if BG then BG.xxx() end`，`BiaoGe: AuctionWA.lua:816, 2117`），纯 WA 场景也能降级运行。

### 项目结构（建议）

```
GameLobby/                       # 核心框架（独立插件）
├── GameLobby.toc                # ## Interface: 38001；不依赖大脚
├── Core/
│   ├── Init.lua                 # 引导队列 + 事件分发（仿 BiaoGe）
│   ├── Bootstrap.lua            # _G.GameLobby 幂等 + 版本门控 + aura_env 双环境
│   ├── Comm.lua                 # 协议编解码、收发、版本探测、状态同步
│   ├── Roster.lua               # 团长/助理识别、团队名单
│   ├── Match.lua                # 比赛状态机（发起→报名→倒计时→比赛→汇总→平局）
│   ├── GameRegistry.lua         # RegisterGame + 游戏列表
│   ├── GameImport.lua           # 「导入游戏字符串」：WA 串解析 + 提取游戏代码 + 信任确认门 + 注册
│   ├── Stats.lua                # 账号级战绩
│   └── UI/                      # 主面板 / 比赛窗 / 结果窗 / 战绩窗 / 导入框 / 关于
├── Games/
│   └── SpeedClick.lua           # 极速按键（同体，可被核心 .toc 加载）
├── Libs/                        # LibSerialize / LibDeflate / LibBase64 / ChatThrottleLib（内嵌，不依赖大脚）
└── dist/
    ├── GameLobby-bundle.wa.txt  # 全家桶（核心+极速按键，WA group 聚合，主推入门串）
    ├── GameLobby-core.wa.txt    # 仅核心 WA 导出
    └── SpeedClick.wa.txt        # 仅极速按键 WA 导出
```

### 架构不变量（不可破坏）

1. **同体机制**：核心与每个游戏都必须 `aura_env or {}` 双环境兼容 + `_G.GameLobby` 版本门控幂等。
2. **游戏逻辑与通讯解耦**：游戏只产出分数 / 事件，收发走核心 `Comm`，新增游戏不改通讯层。
3. **裁判端与参与端职责分离**：汇总排名只在 host 端做；参与端只上报与展示。
4. **协议字节级一致**：插件版与 WA 版收发格式、前缀、gameId 完全相同，才能同场互通。
5. **不依赖 BigFoot / 大脚**：BiaoGe 依赖大脚整合包（`BiaoGe: BiaoGe.toc:8`），本项目要独立可用，自带所需库。

### WA 导出与构建（仍是 M1 的主要风险）

- WeakAuras 导入字符串 = `LibSerialize` → `LibDeflate` → base64 且需符合 WA 表结构，**纯离线生成较复杂**。M1 采用「半自动」流程：游戏内 WeakAuras 建自定义代码 aura，把仓库里的核心 / 游戏 Lua 贴入 custom code，导出字符串落 `dist/`。
- 仓库里的 `.lua` 始终是真相源，WA 字符串是**生成产物**；流程必须可重复、有文档；后续可探索脚本化全自动生成。
- **单一格式 = WeakAuras 格式**（功能 9 决策 A）。`Core/GameImport.lua` 在插件侧**自行解析 WA 串**（`!WA:` → base64 解码 → `LibDeflate` 解压 → `LibSerialize` 反序列化 → 从我们自控的固定字段取游戏代码 → loadstring + RegisterGame），使纯插件用户无需 WeakAuras 也能导入。导出端与解析端共用同一套 WA 模板，降低耦合。
- **三档供给（决策 D16）**：WA 串以三种形态分发，**并存不互斥**，落到 `dist/`：
  - `GameLobby-bundle.wa.txt` — **全家桶**：用 WeakAuras **group（群组）聚合**为单串，含「核心 + 极速按键」两个子 aura。WA 群组导出本就是单串、导入一次装好全部子项。**主推入门串**：新人复制一串、粘贴即玩，直接服务传播性一等目标（§1）。
  - `GameLobby-core.wa.txt` — 仅核心：给想先要壳、游戏后补的人。
  - `SpeedClick.wa.txt` — 仅游戏：给已有核心者加新游戏，**免重发核心**（承接功能 9「新游戏免重装」）。
- **聚合串的加载顺序处理**：全家桶里核心与游戏是两个独立子 aura，**运行先后不保证**。游戏子项调用 `RegisterGame` 时若 `_G.GameLobby` 核心尚未就位，先把 def 压入**待注册队列**（pending queue），核心引导完成后回收注册（配合游戏侧 `if GameLobby then ... end` 的优雅降级，见上文「框架 + 可插拔游戏」）。版本门控**逐组件独立**：已装核心的插件用户导入全家桶时，核心子项按版本门控自动让位（不降级/不重复），游戏子项照常注册，互不冲突。

---

## 6. 通讯协议设计（替代传统 API 设计）

> 无 HTTP API，跨客户端协作走 addon message。格式对齐 BiaoGe 已验证的实践（`BiaoGe: AuctionWA.lua:2026-2152` 的 `CHAT_MSG_ADDON` 处理）。

- **前缀（prefix）**：`GameLobby`，启动时 `C_ChatInfo.RegisterAddonMessagePrefix` 注册。
- **频道**：团队广播用 `RAID`；点对点状态同步用 `WHISPER`。
- **编码**：`命令,arg1,arg2,...` 逗号分隔，接收端 `strsplit(",", msg, 8)`（仿 BiaoGe）。名字带 `-realm`，约定字段内不含逗号。
- **版本探测**：`VersionCheck` / `MyVer` 互探，版本不兼容给可读提示（功能 9）。

### 消息类型

| 命令 | 方向 | 载荷 | 说明 |
|------|------|------|------|
| `Start` | host → RAID | matchId,gameId,gameVer,hostName,joinDeadline,duration | 发起开赛（含 gameId/版本，缺该游戏者据此提示） |
| `Join` | 参与端 → RAID | matchId,playerName | 报名参与 |
| `Result` | 参与端 → RAID | matchId,playerName,score,round | 上报本轮分数（带上限校验，功能 5） |
| `Final` | host → RAID | matchId,winner,rankingCSV | 最终排名（rankingCSV：`名字:分数` 逗号分隔，过长截断前 N 名） |
| `Tie` | host → RAID | matchId,tiedNamesCSV,round,duration | 触发突然死亡加赛（功能 6） |
| `GetState` | 晚加入者 → RAID | （无） | 询问当前是否有进行中比赛 |
| `State` | 在场者 → WHISPER | matchId,gameId,phase,remaining,... | 把当前比赛状态回传晚加入者 |
| `VersionCheck` | 任意 → RAID | （无） | 请求各端回报版本 |
| `MyVer` | 任意 → RAID | coreVer,gameListCSV | 回报核心版本与已装游戏 |

### 状态恢复（晚加入 / 重载）

进入世界或 `/reload` 后，客户端广播 `GetState`；在场者用 `WHISPER` 回传 `State`，晚加入者据此补建当前比赛 UI。直接对应 BiaoGe 的 `GetAuctioning` / `Auctioning` 机制（`BiaoGe: AuctionWA.lua:468-475, 2127-2149`）。

### 消息示例

```
Start,1716800000-Tank,speedclick,1.0,Tank,10,10
Join,1716800000-Tank,Healer
Result,1716800000-Tank,Healer,187,0
Final,1716800000-Tank,Healer,Healer:187,Tank:165,Dps:150
Tie,1716800000-Tank,Healer,Dps,1,5
```

### 协议约束

- [ ] 单条消息 ≤ 255 字节；排名等过长内容截断到前 N 名或分片；用 `ChatThrottleLib` 节流。
- [ ] 只处理「当前进行中 matchId」的消息，过期 / 未知命令丢弃。
- [ ] 喊话公示只由 host（团长/助理）执行 `SendChatMessage(..., "RAID" / "RAID_WARNING")`，避免全团刷屏（对应 BiaoGe `if aura.IsRaidLeader() then SendChatMessage(...) end`，`BiaoGe: AuctionWA.lua:811-815`）。
- [ ] 收到 `Start` 但本端缺该 `gameId` 或版本过旧 → 提示用户索取 WA 字符串，不静默失败。

---

## 7. 验收标准

### 完成标准（M1）

- [ ] 独立插件可在 WLK 时光服正常加载（无 Lua 报错），`/gl` 开关面板。
- [ ] 团长/助理能发起极速按键，全团收到弹窗。
- [ ] 参与者能在 10 秒窗口内点击计数，结束自动上报。
- [ ] 裁判端正确汇总并广播排名，全团看到一致结果，冠军高亮。
- [ ] 冠军并列时正确触发 5 秒加赛直至分出唯一冠军。
- [ ] 个人战绩按账号正确累计与展示，可清空。
- [ ] 提供**全家桶** WA 字符串（核心+极速按键，group 聚合）：新人一次导入即得可玩大厅 + 极速按键，无需先装核心（决策 D16）。
- [ ] 提供**大厅核心**的 WA 导入字符串，导入后功能等价于插件。
- [ ] 提供**极速按键**的独立 WA 字符串，已装大厅者导入后游戏立即出现并可玩，无需 /reload。
- [ ] 已装核心的用户导入全家桶：版本门控令核心子项自动让位（不降级/不重复），游戏子项照常注册。
- [ ] 导入更高版本的游戏 WA 字符串可热升级（版本门控替换旧版）。
- [ ] WA 用户与插件用户可同场互通；团内缺该游戏者收到索取提示。
- [ ] 晚加入 / `/reload` 的客户端能通过状态恢复补建进行中的比赛 UI。
- [ ] 「关于」面板可打开，显示作者联系方式（只读可复制）+ 可选赞助二维码。
- [ ] 占位游戏显示「即将推出」且不可发起。

### 测试用例

| 用例 | 步骤 | 预期结果 |
|------|------|----------|
| 正常一局 | 团长发起 → 3 人参与 → 各自点击 → 结束 | 排名正确，最高分者为冠军并公示 |
| 平局加赛 | 两人点击数相同且并列第一 | 触发 5 秒加赛，分出唯一冠军 |
| 非团长发起 | 普通团员点发起 | 按钮禁用 / 提示「仅团长/助理可发起」 |
| 无插件成员 | 团内有人未装插件 | 该成员无弹窗，裁判端显示「X 人无插件无法参与」 |
| 围观 | 选择「围观」 | 看到比赛窗口但按钮不可用，能看最终结果 |
| 异常分数 | 上报超过物理上限的分数 | 标记异常、不计入冠军 |
| 不在队伍 | 单人时打开面板 | 发起禁用并提示「需在队伍或团队中」 |
| 插件×WA 互通 | 一人插件、一人 WA 同场 | 双方都能正常参与、看到一致排名 |
| 全家桶入门 | 全新用户（无插件无 WA）导入全家桶 WA 串 | 一次导入即得可玩大厅 + 极速按键，无需先装核心、无需 /reload |
| 全家桶幂等 | 已装核心者再导入全家桶 | 核心子项按版本门控让位不重复，游戏子项正常注册，无冲突 |
| 新游戏分享 | 已装大厅者导入新游戏 WA 字符串 | 游戏立即进列表、可发起，无需 /reload |
| 游戏热升级 | 导入更高版本同游戏 WA | 版本门控用新版替换旧版，旧实例被卸载 |
| 缺游戏提示 | host 发起某游戏，团员未装该游戏 | 团员收到「索取 WA 字符串」提示，不报错 |
| 状态恢复 | 比赛中途 `/reload` | 重载后补建当前比赛 UI 并继续 |

---

## 附录

### 术语表

| 术语 | 定义 |
|------|------|
| 裁判 / host | 发起本场比赛、负责汇总排名的客户端 |
| matchId | 单场比赛唯一标识 |
| 时光服 | 中国大陆运营的怀旧 WoW 服务器（本项目目标 WLK） |
| WA | WeakAuras 插件 / 其导入字符串 |
| addon message | WoW 隐藏的插件间通讯消息 |

### 待核实项

见 [requirements.md §8](requirements.md)（TOC Interface 号、`C_ChatInfo` 可用性、StaticPopup 限制、消息长度/节流、WA 字符串生成流程）。

### 非目标

防作弊、自动分配战利品、跨团队持久共享排行榜、占位游戏的可玩逻辑（详见 [requirements.md §7](requirements.md)）。
