# M1 状态报告（2026-05-27 夜，自主多 agent 开发）

> 30 秒速览：**M1 全部功能代码已完成**，并在「无头 WoW 模拟环境」端到端跑通（加载/五屏/一整局/线路收发/平局加赛/战绩/导入导出，**零运行时错**）。主界面已在时光服真机打开可用。
> 剩下的是**只能真机/多人/真 WeakAuras 验证**的部分（见 §3），代码已就位、待你真机确认。
> 真相源仍是 [SPEC.md](../SPEC.md)；接口契约见 [contracts.md](contracts.md)。

---

## 1. 验证手段（本仓库可复跑）

本机有 Lua 5.1（`C:\Program Files (x86)\Lua\5.1\lua.exe`），三个无头测试覆盖逻辑+集成：

| 测试 | 覆盖 |
|------|------|
| `GameLobby/Tests/Match_selftest.lua` | 状态机逻辑：排名/平局加赛/异常分/非host不裁决/prize往返/Live节流/战绩 |
| `GameLobby/Tests/run_all.lua` | 加载全插件 + 引导flush + slash注册 + 五屏渲染 + host一整局 + 战绩 + 邀请/倒计时/导入导出面板 |
| `GameLobby/Tests/run_match.lua` | **线路收发**：参与端收Start→Join→Begin→倒计时→上报→收Final落战绩；host收并列最高分→触发Tie加赛 |

`GameLobby/Tests/headless_env.lua` = WoW 3.3.5 API 模拟（frame/纹理/事件/C_Timer/通讯/颜色对象/bit 等）。
**部署脚本已把这三个测试设为发布前门禁**（`/deploy-addon`，语法+测试不过则中止）。

> 局限：模拟环境把贴图/SetGradient/字体度量 no-op，只查**逻辑/nil/字段**错，**不查真机视觉、真实网络、真 WeakAuras**。

---

## 2. SPEC §7 验收标准 —— 代码完成 + 无头验证

| # | 验收项 | 状态 |
|---|--------|------|
| 1 | 插件加载无报错、`/gl` 开面板 | ✅ 真机已确认 |
| 2 | 团长/助理发起、全团弹窗 | ✅ 代码完成 + 线路测试过（待真机多人） |
| 3 | 10 秒点击计数、结束上报 | ✅ 逻辑完成（OnMouseDown 计数待真机手感） |
| 4 | 裁判汇总排名、全团一致、冠军高亮 | ✅ 线路测试过（待真机多人一致性） |
| 5 | 冠军并列触发 5 秒加赛至唯一冠军 | ✅ 自检 + 线路测试过（多轮收敛验证） |
| 6 | 个人战绩账号累计、可清空 | ✅ 完成 + 测试过 |
| 7/8/9 | 全家桶/核心/极速按键 **WA(!WA:2!) 串** | ⚠️ 需真机 WeakAuras 生成（见 §3） |
| —  | 极速按键 **!GL: 自控串**（大厅导入框） | ✅ **已离线生成并往返校验**：`dist/SpeedClick.gl.txt` 可直接用 |
| 10 | 版本门控热升级 | ✅ 同体/门控逻辑完成（待真机导入验证） |
| 11 | WA×插件同场互通 | ⚠️ 需多客户端真机 |
| 12 | 晚加入/reload 状态恢复 | ✅ 逻辑完成（待真机多人） |
| 13 | 关于面板 | ✅ 完成 |
| 14 | 占位游戏「即将推出」 | ✅ 完成 |

**本夜多 agent 修复的真 bug**：
- Match：`joinDeadline` 报名截止保护从未生效（会卡死「等全员就绪」）；`noAddon` 无插件统计无数据源 → 均补全。
- UI：战史图标把贴图路径当文字显示；`MATCH_CLOSED` 后参与端卡在比赛/结算屏；`MATCH_TIE` 全无界面反馈；奖品卡可编辑性不随团长/团员身份切换 → 均修复。
- 安全：`GameImport.CollectGames` 对循环引用 bundle 无界递归 → **信任门之前**栈溢出崩溃 → 加 visited 去重修复。

---

## 3. 只能真机 / 多人 / 真 WeakAuras 验证（明天你来）

1. **真机跑一整局**：组队/团队，团长发起极速按键 → 全员弹窗 → 准备 → 倒计时 → 10 秒狂点 → 排名一致、冠军公示。
2. **平局加赛**：两人并列第一 → 5 秒加赛。
3. **WA(!WA:2!) 串生成**：`dist/` 的三档 `.wa.txt` 仍是占位，需游戏内 WeakAuras 半自动导出（流程见 `dist/README.md`）。
4. **!GL: 串导入回环**（现在就能测）：右键极速按键→导出，复制；或直接用 `dist/SpeedClick.gl.txt` → 大厅「+导入游戏」粘贴 → 信任确认 → 立即可玩。
5. **插件×WA 互通 / 热升级 / 晚加入恢复**：需多客户端。
6. **视觉细节**：金属边框/狂点钮发光/倒计时动画/castbar 火花/字距等（模拟环境看不到）。
7. **无插件统计准确性**：当前用「未回 Join 推断」，挂机未准备者会被误判；若不可接受可升级为 VersionCheck 驱动（handler 已就位）。

---

## 4. 抓错办法（时光服屏蔽了报错弹窗）

```
/run local ok,err=pcall(GameLobby.UI.Toggle, GameLobby.UI); print("结果:", ok, err)
```
任何「点了没反应」都这样把错误打到聊天框，截图发我。详见 memory `timewalking-server-quirks`。
