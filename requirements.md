# 原始需求 & 决策记录 — 游戏大厅（RaidGames）

> SDD 阶段 1 产出。本文件记录需求来源与讨论中拍板的决策。
> 正式可测试规格见 [SPEC.md](SPEC.md)（唯一真相源）。
> 记录日期：2026-05-27

---

## 1. 一句话定位

一个魔兽世界（WotLK Classic / 时光服）插件「游戏大厅」：团本打完后，用小游戏比赛代替单纯 roll 点来决定战利品归属，让运气差的人有凭手速翻盘的机会。**排名和战绩是核心体验。**

## 2. 目标用户

- 中国魔兽世界**时光服**玩家
- 团队/公会场景，尤其是 roll 点运气差、想要二次机会的人
- **团长 / 团队助理** = 大厅管理员（发起比赛）
- **团员** = 收到弹窗后参与或围观

## 3. 核心痛点

roll 点纯看脸，输的人不服气 → 用「技术 / 手速」类小游戏让人输得心服口服。

## 4. 角色与流程（口头描述）

1. 团长/助理在大厅选择一款游戏并发起
2. 全团装了插件/WA 的人收到弹窗，可选择参与或围观
3. 倒计时后开始比赛
4. 比赛结束，汇总排名并公示冠军
5. 团长据此手动把战利品给冠军

## 5. 游戏清单

| 游戏 | 状态 | 说明 |
|------|------|------|
| 极速按键 | **M1 实现** | 10 秒内比谁点得多，多者胜 |
| 是男人就下 100 层 | 占位，暂不做 | 预留游戏槽位 |
| 是男人就上 100 层 | 占位，暂不做 | 预留游戏槽位 |

## 6. 已拍板的关键决策

| # | 议题 | 决策 |
|---|------|------|
| D1 | 极速按键的「按」 | **狂点屏幕上的大按钮（鼠标）**，计数 100% 可靠、实现最简单 |
| D2 | 排名 / 记录范围 | **即时排名 + 本地个人战绩**（历史最高分、胜场等，存 SavedVariables）；暂不做玩家间同步的共享榜 |
| D3 | 战利品归属 | 插件**只当裁判**（判定 + 公示 + 记录），团长手动给装备 |
| D4 | 客户端版本 | **巫妖王之怒 WLK（WotLK Classic）**，时光服报版本 `3.80.1.67621`；现代 Classic API 可用 |
| D5 | 发起权限 | **团长 + 团队助理（assist）** 可发起 |
| D6 | 平局规则 | **突然死亡加赛 5 秒** |
| D7 | M1 交付范围 | **独立插件 + WeakAura 字符串一步到位**（两种分发都要可用，且能在同一场比赛里互通） |
| D8 | 个人战绩范围 | 按**账号**累计（跨角色） |
| D9 | 无插件团员 | 收不到弹窗，自动不参与；提示发起者「X 人无插件无法参与」 |

## 7. 非目标（明确不做）

- **不防作弊**：分数由各客户端自报，纯客户端插件无法真正防篡改；只做轻量上限校验（点击数不能超过物理上限），靠团队社交信任。
- **不自动分配战利品**：WoW 拾取分配受保护，由团长手动操作。
- **不做跨团队 / 公会持久共享排行榜**（M1 范围内）。
- ~~不做游戏内一键推送 / 团内 P2P 代码传输~~ **（2026-05-31 D17 反转，移出非目标）**：原因见 D17——"方便分享"是本项目存在的根本意义，BiaoGe 在本服已实测可行（截图佐证）。**注意边界**：P2P 推送只用于把**单个小游戏**推给**已装大厅核心**的人；大厅本体（核心）因"鸡生蛋"（无监听端收不到）+ 体量过大，第一次传播**仍只能靠 WA 串**，不走 P2P。详见 SPEC §6「点对点游戏推送」。
- 占位游戏「上 / 下 100 层」不实现，仅预留游戏注册槽位。

## 8. 待核实项

通过研读同类插件 **BiaoGe（金团表格）源码**（`sample/BiaoGe/`），以下原"待核实"已确认：

- ✅ **TOC `Interface` 号 = `38001`**（时光服 `3.80.1.67621` → 3.80.x → 38001，见 `BiaoGe.toc:1`）。
- ✅ **`C_ChatInfo.SendAddonMessage` / `RegisterAddonMessagePrefix` 可用**，无需旧版兜底（`AuctionWA.lua` 全程使用）。
- ✅ **`StaticPopupDialogs` 可用**（`AuctionWA.lua:1012`）。
- ✅ **`ChatThrottleLib` 可用**（`Receive.lua:454` 等在用），用于节流/分片。
- ✅ **"插件 + WA 同体"机制已验证**：`aura_env or {}` + `_G.全局` 版本门控幂等（见 SPEC §5）。

仍待确认：

- WA 导出字符串的具体生成流程（仍需在游戏内 WeakAuras 构建并导出，见 SPEC §5「WA 导出与构建」）。
- 时光服是否对 addon message 频率有额外限制（以 ChatThrottleLib 兜底）。

> 参考样本：`sample/BiaoGe/`（同类金团插件，团长发起→团员弹窗→决定战利品，且插件/WA 双分发，是本项目最直接的架构蓝本）。

## 9. 额外工作流（非游戏功能）

- 为项目添加一批「魔兽世界插件开发」专用 agent（Lua/XML、WoW API、addon 通讯、WA 打包），放 `.claude/agents/`。SPEC 定稿后建。

## 10. 设计稿（UI）引入的新决策

来源：用户用 Claude Design 出的设计稿，handoff bundle 在 `sample/RaidGames-handoff/`。已据此重写 SPEC §4 UI/UX，并把以下能力接入 §2/§3。

| # | 议题 | 决策 | 状态 |
|---|------|------|------|
| D10 | 视觉风格 | 暗色 + 暴雪式金属边框窗体。3 套主题令牌已在 SPEC §4.1 留存；**M1 只实现默认铁木 Ironwood**，主题切换（奥术/血色）降级到后续里程碑 | ✅ 已定（M1 仅铁木） |
| D11 | 奖品三态 | ①真实战利品（稀有度色）②团长**自定义文字奖品**（≤60 字）③**友谊赛**（无奖品）——**全部纳入 M1**（扩展原 D3） | ✅ 已定（全纳入） |
| D12 | 参与流程 | 邀请弹窗 → 进大厅点「**准备**」→ 团长见全员就绪开局（采纳设计流程，更新功能 3） | ✅ 已定 |
| D13 | 展示细节 | 结算榜显示 **CPS**（点击/秒）；比赛中实时排名 live-board；底部**日志条**；战史 5 统计卡（总场次/胜场/胜率/奖品收获/平均分） | ✅ 已纳入 |
| D14 | 插件用户装游戏 | 大厅插件**自带「导入游戏字符串」框**，纯插件用户（没装 WeakAuras）也能粘字符串装游戏，不必装游戏子插件文件 | ✅ 已定 |
| D15 | 字符串格式 | **统一用 WeakAuras 格式（一串通吃）**：WeakAuras 用户粘进 WA；纯插件用户粘进大厅，大厅**内置 WA 串解析**（base64+LibDeflate+LibSerialize，取自控固定字段的游戏代码）。联机互通靠协议、与字符串格式无关 | ✅ 已定（方案 A） |
| D16 | WA 串供给档位 | **三档并存，不互斥**：①**全家桶**（核心+极速按键，WA group 聚合为单串）= 新人一串入门，**主推**；②仅核心（先要壳、游戏后补）；③仅游戏（老用户加新游戏，免重发核心，承接 D14「新游戏免重装」）。group 聚合的加载顺序问题用「游戏先进待注册队列、核心引导后回收」解决，版本门控逐组件、互不干扰 | ✅ 已定 |
| D17 | 点对点游戏推送（反转原非目标） | **加入**「游戏内把单个小游戏直推给指定玩家」：照 BiaoGe 成熟打法（`!标记!`包裹 → 切 ≤255 字节 → ChatThrottleLib 逐块 WHISPER → 接收端按标记重组）。**两条分享车道**：①整包 WA 串（给"啥都没有"的新人，第一次上车，唯一可行）；②P2P 推送（给"已装核心"的老人，快速加游戏）。**边界**：核心本体不走 P2P（鸡生蛋+体量大）；先握手（声明游戏+大小）→ 接收端走信任门同意 → 才发分片，复用 GameImport 的 ParseWA/信任门/RegisterGame | ✅ 已定（M1+ 实现中） |
| D19 | **回插件路线（暂停 WA 版，反转 D18）** | 真机实测结论：把完整核心当纯 WeakAuras 跑，会被 WeakAuras 5.x 沙箱（`AuraEnvironmentWrappedSystems`）全面拦截——`loadstring` 禁用、`/命令` 注册不进真实命令表、命名框架不落真实 `_G`、无 SavedVariables；这是 WA 故意"不让光环变完整插件"的安全设计，硬怼脆弱且一墙接一墙。故**开发回到插件路线**：核心=插件（装一次），"插件之间互相传播小游戏"用**已实现**的两条：①右键队友 P2P 聊天推送（D17）②右键游戏格导出 `!GL:` 串、对方大厅导入框粘入。**WA 路线暂停**：`dist/` 的 WA 打包脚手架（`build_bundle.py`/`verify_bundle.lua`/`HOW-TO-EXPORT-BUNDLE.md`/`*.wa.txt`/`_bundle-*.paste.txt`）**已删除**（git 历史可恢复）；代码内同体机制（`aura_env`/`isWA` 检测）、`isWA` 图标启动器分支、`Init.lua` IsLoggedIn 兜底、`Core/ShareBundle.lua`+`GL.UI:ShowShare` **惰性保留**（插件路径 isWA=false 不触发，零影响）。日后若重启 WA：BiaoGe 式"插件主持 + 轻量参与者 WA"是更可行形态，而非整包核心当 WA。`dist/` 仅留插件路线有用的 `SpeedClick.gl.txt` + `export-tool.lua`。 | ✅ 已定（插件优先） |
| D18 | **WA 版优先**（调整 D7 重心；**已被 D19 反转，见上**） | 真机验证发现：纯插件分发"使用成本高、难传播"，而 WeakAuras 串"复制即用"才是传播核心。故**开发重心转向 WA 版**（插件版因同体机制零成本保留、不废弃）。WA 版的沙箱约束与对策（真机实测得出）：①WeakAuras 5.x 沙箱**禁 loadstring** → 打包 WA 版游戏须**直接定义 def、不用 loadstring**（插件版不变）；②沙箱**注册不进真实 `/命令`** → WA 版用**屏幕图标启动器**开大厅（`isWA` 时建图标、跳过 slash）；③WA 当不了 P2P 推送的**接收端**（收到代码跑不起来）→ WA 用户靠 **WeakAuras 导入串**收新游戏（WeakAuras 自己编译，不需我们 loadstring），传播照常；④WA **无 SavedVariables** → 战绩等持久化在 WA 版降级为会话内（或后续接 WA 存储）。互通不受影响（协议一致，同场可玩）。 | ✅ 已定（开发中） |
| D20 | **只分享单个小游戏，彻底放弃整包（核心）分享**（收敛 D16） | 用户拍板：不需要"分享整个游戏大厅（核心）"，只要**在用户之间传播单个小游戏**即可。这把 D16「三档供给（①全家桶 ②仅核心 ③仅游戏）」收敛为**只保留第 ③ 档：单游戏分享**。理由：核心本就走插件安装一次（D19），整包 WA 串路线又被沙箱拦死（D19），"整包分享"既无可行实现又非必要。**已落地（2026-06-01）**：①删除 `Core/ShareBundle.lua`（整包 WA 串数据源）、`GL.UI:ShowShare()`（整包分享弹框）、`.toc` 加载行、`run_all.lua` 整包测试步——`git rm` + 三个无头测试仍全绿；②保留三个**单游戏**分享入口（已实现且是 M1 主路径）：标题栏「分享」按钮逐个列游戏导出 `!GL:` 串、游戏格右键「导出字符串」、队友卡右键 P2P 推送（D17）；③`Core/GameImport.lua` 的多游戏 `kind="bundle"` **解析**能力保留（那是"导入多个游戏"而非"分享大厅核心"，且含循环引用安全修复，无害留着），但导出端不再主动生产整包。**边界澄清**：D20 放弃的是"分享核心/整包"，不是"多游戏聚合串"；新人第一次上车仍靠分发插件本体。 | ✅ 已定（已落地） |
| D21 | **框架从「刷分专用」泛化为「通用比赛容器」+ 分层 api + 游戏元数据契约** | 用户提出未来要做**复杂小游戏**（如「是男人就下 100 层」这类实时动作游戏：自定义画面 + 逐帧循环 + 键盘输入 + 「越深越高」而非「点得越多越高」）。M1 框架隐含假设了**单一形态**——固定时间窗口 + 狂点累加分（`api:SmashButton()` + 写死的 castbar+计数 PlayingScreen），这套对下 100 层完全套不上。**决策**：把框架升级为只管比赛流水线（发起→报名→倒计时→比赛→收分→排名→平局→结算）的**通用容器**，把「比赛画面」整块让给游戏。落地三件事：①**分层 api**——`tier="score"`（刷分档，框架给标准屏+控件，门槛极低）vs `tier="canvas"`（自绘档，框架只给一块空 Frame，opt-in）。canvas 在 WoW 里只是 `CreateFrame("Frame")` 容器，非像素画布，零额外重量；每端只渲自己一局（O(1)，与团队人数无关），性能可控。②**游戏自报元数据**——`endMode`(timed/elimination/race) / `scoreOrder`(desc/asc) / `scoreUnit` / `scoreCap` / `seeded` / `needsKeyboard`，使框架能通用地裁决排名/校验/展示，不再硬假设。③**统一生命周期**——`setup/start/stop/teardown/onTie/onResult` 取代 M1 的 `client/host`；新增 `api:Canvas()` / `api:CaptureKeyboard()`（键盘焦点框架托管，防游戏锁死玩家移动键） / `api:GetSeed()`（统一随机种子保公平） / `api:Finish()`（elimination 主动结束）。**规范产出**：面向第三方/未来游戏作者的 [docs/game-dev-spec.md](docs/game-dev-spec.md)（与内部 contracts.md 分开）。**节奏**：本决策落地放 **M2**——本 commit 只定规范（文档），下 commit 用「是男人就下 100 层」作首个 canvas 档游戏实现并验证规范，同时把极速按键迁移到统一生命周期。键盘捕获在时光服的稳定性是 canvas 路线的最大技术风险点，M2 实现前先做最小真机 spike 验证。**不变量不变**：游戏只产出分数/事件、收发走核心 Comm（#2）；协议字节级一致（#4）；自包含源码可分享（#1 同体）全部保留。 | ✅ 已定（M2 落地，本 commit 仅定规范） |
